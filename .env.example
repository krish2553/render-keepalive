TARGET_URL=https://your-render-service.onrender.com/health
KEEPALIVE_METHOD=GET
TIMEOUT_MS=15000

# Optional: set when your health endpoint requires auth.
# KEEPALIVE_AUTH_TOKEN=your-token

# Optional: set when success is a specific status instead of any 2xx.
# EXPECTED_STATUS=200
